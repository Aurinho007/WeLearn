import styled from "styled-components";

export const ClassrommCardContainer = styled.div`
  display: grid;
  grid-template-columns: repeat(4, 350px);
  gap: 25px;
  justify-items: center; 
  justify-content: center; 
  width: 100%;
`;
